# Secrets
.env
.cache_key

# Index persisté (regénéré au démarrage)
index_cache.json
index_cache.npz
index_cache.hmac

# Base SQLite des cartes de révision
revision.db

# Documents uploadés
docs/*

# Python
__pycache__/
*.pyc
venv-rag/
*.egg-info/
!docs/.gitkeep

# PDF de démo/fallback à la racine (ne pas committer de matériel de cours)
/*.pdf
